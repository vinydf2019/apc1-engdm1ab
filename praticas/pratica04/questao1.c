/*1. Faça um programa em C que preencha uma matriz com 10 números, depois leia
 * um número e localize a posição dele na matriz.
 */
#include <stdio.h>
#define LIMITE 10

int main() {
  
  int numero[LIMITE];      // Número a ser procurado na matriz
 
  // Preenchendo a matriz com 10 números
  printf("Digite o número a ser encontrado para preencher a matriz:\n");
  for (int i = 0; i < LIMITE;  i++) {
    
     
    }
  }

  // Lendo o número a ser localizado
  printf("Digite o número a ser localizado na matriz: ");
  scanf("%d", &numero);

  // Localizando a posição do número na matriz
 
  // Se o número não foi encontrado
  

  return 0; // Indica que o programa terminou com sucesso
}